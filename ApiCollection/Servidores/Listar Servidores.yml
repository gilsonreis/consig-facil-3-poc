info:
  name: Listar Servidores
  type: http
  seq: 2

http:
  method: get
  url: "{{base_url}}/api/v1/servidores"
  headers:
    - name: Authorization
      value: Bearer {{token}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
